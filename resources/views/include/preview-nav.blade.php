
<preview-nav
        :time-remain="{{json_encode($timeRemain)}}"
        :test="{{json_encode($test)}}"
        :test-questions="{{json_encode($testQuestions)}}"
        :count-down="{{json_encode($countDown)}}"

>

</preview-nav>
